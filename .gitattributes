##############################
# 全局基础配置
##############################
* text=auto                  # 自动检测文本文件类型
*.meta text eol=lf           # 强制 Meta 文件使用 LF
ProjectSettings/*.asset text # 项目设置保持文本属性

##############################
# Git LFS 管理规范（按优先级分层）
##############################
# [Tier 1] 必须使用 LFS 的核心资源
# ----------------------------------
*.fbx filter=lfs diff=lfs merge=lfs -text       # 3D 模型
*.psd filter=lfs diff=lfs merge=lfs -text       # Photoshop 源文件
*.exr filter=lfs diff=lfs merge=lfs -text       # 高动态范围纹理
*.blend filter=lfs diff=lfs merge=lfs -text     # Blender 工程文件
*.mp4 filter=lfs diff=lfs merge=lfs -text       # 视频素材

# [Tier 2] 按需使用 LFS 的通用资源
# ----------------------------------
*.tga filter=lfs diff=lfs merge=lfs -text       # 未压缩纹理
*.wav filter=lfs diff=lfs merge=lfs -text       # 无损音频
*.zip filter=lfs diff=lfs merge=lfs -text        # 资源包

# [Tier 3] 建议普通版本控制的文件
# ----------------------------------
*.png -filter                    # 小尺寸 PNG 无需 LFS
*.jpg -filter                    # 压缩位图直接版本控制
*.ttf -filter                    # 字体文件（通常 <5MB）
*.svg -filter                    # 矢量图形按文本处理

##############################
# 二进制文件处理策略
##############################
# Unity 编译产物
*.dll binary
*.so binary
*.bundle binary

# 开发环境文件
*.pdb binary                     # 调试符号文件
*.mdb binary                     # Mono 调试数据库

##############################
# 高级合并策略
##############################
# Unity 专用合并工具
*.unity merge=unityyamlmerge     # 场景文件合并
*.prefab merge=unityyamlmerge   # 预制件合并
*.asset merge=unityyamlmerge     # ScriptableObject 合并

# 禁止自动合并的文件类型
*.controller -merge              # Animator 控制器
*.mixer -merge                   # 音频混合器